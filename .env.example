# Copy to .env in the project root and fill in real values. Never commit the filled file.

# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=

# MapTiler key for the disaster map (optional)
VITE_MAPTILER_KEY=
